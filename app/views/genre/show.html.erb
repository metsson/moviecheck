<% if @movies %>
<% content_for :og_tags do %>
<meta property="og:title" content="All <%= @genre.genre.downcase %> movies.">
<meta property="og:description" content="A list of <%= @genre.genre.downcase %> movies on filmcheck.">
<meta property="og:url" content="<%= request.original_url %>">
<meta property="og:site_name" content="filmcheck">
<meta property="og:type" content="video.movie">
<% end %>

<% content_for :title, "#{@genre.genre} movies on" %>

<h2>Showing <%= @genre.genre.downcase %> movies</h2>
<div class="content">
    <div class="movie-listing">
        <% for movie in @movies %>
            <%= link_to show_movie_path(movie.title.parameterize, movie.imdb), :class => 'movie-listing-item' do %>
             <span class="listing-point"><%= '%.1f' % movie.score %> <em>of 10</em></span> <%= movie.title %>
            <% end %>
        <% end %>
    </div>
</div>
<% else %>
<p>Shit hit the fan! The genre you searched for does not exist...</p>
<% end %>